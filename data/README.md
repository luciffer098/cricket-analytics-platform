# Data Directory

This directory contains cricket data for the analytics platform.

## Purpose
- Store raw cricket data (ball-by-ball data, match information, player statistics)
- Maintain data files in structured formats (CSV, JSON, Parquet, etc.)
- Serve as the source for ETL processes

## Structure
- Add subdirectories for different data types (raw, processed, archived)
